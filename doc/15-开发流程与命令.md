# 开发流程与命令

## 日常命令

| 场景 | 命令 |
|---|---|
| 开发 | `wails dev`（前端热更新 + Go 热重启） |
| 仅前端调样式 | `cd frontend; npm run dev`（浏览器打开，调不到 Go 绑定） |
| 构建 | `wails build` |
| 后端测试 | `go test ./...` |
| 后端边界校验 | `powershell -File scripts/check-boundaries.ps1` |
| 前端类型检查 | `cd frontend; npm run typecheck` |
| 前端测试 | `cd frontend; npm run test` |
| 运行时复制 | 构建后自动执行 `scripts/copy-runtimes.ps1` |

## 测试地图

测试按「复杂流程 / 长链路 / 安全边界」取舍，不做逐行覆盖。改某处代码时按此表定位对应测试，不必全量跑读。

### 后端

| 测试文件 | 守护的链路 |
|---|---|
| `harness/runner_test.go` | ReAct 主循环、工具闸门四档权限、子 Agent 委派上下文隔离、检查点续跑、停滞 / 截断重试 / 注入防护 |
| `harness/compress_test.go` | 上下文预算压缩（工具配对保持、安全切点、runner 集成） |
| `service/chat_test.go` | 会话分叉、审批决定、插话排队、后台任务生命周期、工作区绑定、压缩归档、消息清洗（空 assistant / 孤儿 tool） |
| `service/integration_test.go` | 跨模块：MCP 配置加密与回滚、文件变更快照与回滚、审批恢复 |
| `service/security_test.go` | 目录信任解析 / 授权 / 撤销、路径穿越拦截、内部目录隐藏 |
| `server/server_test.go` | SSE：连接、Last-Event-ID 重放、gap 兜底、慢客户端断开 |
| `mcp/client_test.go` | stdio 握手、工具调用、死进程失败、Manager 重载与启动降级（re-exec fixture，无外部依赖） |
| `workflow/executor_test.go`、`workflow/nodes/llm_react_test.go` | DAG 顺序 / 并行层 / 取消；LLM 节点 ReAct 分支与补全兜底 |
| `memory/memory_test.go` | 形成策略触发器打分、情景检索 |
| `rag/rag_test.go` | 中文导入索引检索、重建索引替换分块 |
| `capability/capability_test.go` | 能力按序注入、单能力失败隔离 |
| `llm/thinking_test.go` | 思维链渲染策略、上游错误分类 |
| `runtime/runtimes_test.go` | 运行时解压的路径穿越与限额 |
| `tool/workspace_link_test.go` | 工作区根解析在 file_write 与 exec 间的一致性 |

### 前端

| 测试文件 | 守护的链路 |
|---|---|
| `chat/__tests__/streamPipeline.spec.ts` | SSE 事件 → 解码 → 批渲染 → 历史块还原 → 终因横幅 全链路契约 |
| `chat/__tests__/merge.spec.ts` | 消息与过程块合并规则 |
| `i18n/__tests__/dict.spec.ts` | 中英字典 key 完整性 |

## 工程约定

- **先读再写**：改前通读目标文件与对应测试；沿用既有模式，不引入第二套做法。
- **最小改动**：外科手术式修改，不顺手格式化无关代码；只清理自己改动产生的冗余。
- **修 Bug 先复现**：能写成测试的先写测试；改完跑受影响包的测试，再跑全量。
- **验证命令**：后端 `go build ./... && go test ./...`，前端 `npm run typecheck && npm run test`。
- **提交信息**：写清场景与根因，不写「fix bug」。

## 调试入口

| 想看什么 | 去哪 |
|---|---|
| run 的完整事件流 | `{home}/runs/{runID}.jsonl`、`GET /chat/runs/:id/events` |
| 一次 LLM 调用的用量 | `token_usages` 表、日志关键字 `chat run done` |
| 工具执行 / 审批 | 事件 `chat:tool*`、`chat:approval*`；审批记录表 |
| 启动是否健康 | `{home}/logs/` 中 `workbaby started`（含工具数 / 技能数 / MCP 就绪数） |
